declare type availableLanguagesTypes = 'pt_br' | 'en';

declare type I18NStrings = {
  common: {
    play: string;
    back: string;
    settings: string;
    quit: string;
  };
  settings: {
    language: string;
  };
};
